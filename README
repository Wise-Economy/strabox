# Strabox

# Run

```
./gradlew run
```

excepts db configuration in application.conf file

```
db {
    jdbcUrl = "jdbc:postgresql://localhost:5432/postgres"
    driverClassName = "org.postgresql.Driver"
    username = postgres
    password = postgres
    maximumPoolSize = 10
}
```